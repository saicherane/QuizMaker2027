# Load .env.development* files when running `wrangler dev`
NEXTJS_ENV=development

# Required for local Workers preview (`npm run preview`) and production deploy.
# Generate with: openssl rand -hex 32
SESSION_SECRET=replace-with-a-random-string-at-least-32-chars
